/**
 * app/index.js
 *
 * Application bootstrap barrel.
 * Re-exports the root App component.
 * Allows future app-level configuration to be centralized here
 * (e.g., error boundaries, analytics initialization, feature flags).
 */

export { default } from '../App'
